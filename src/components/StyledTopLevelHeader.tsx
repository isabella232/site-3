import { Header } from 'semantic-ui-react';
import styled from 'styled-components';

export const StyledTopLevelHeader = styled(Header)`
  font-family: 'Exo 2', sans-serif;
  text-transform: uppercase;
`;

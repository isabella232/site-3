language: node_js

node_js:
  - node

cache: npm

before_install:
  - pip install --user awscli
  - export PATH=$PATH:$HOME/.local/bin

install: npm install

script:
  - npm test

deploy:
  provider: script
  script: npm run deploy
  on:
    branch: master
